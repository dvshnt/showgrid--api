all the document models stored in the database

###artist###
artist schema


###venue-event###
file contains a venue schema and an event schema. event schemas are nested inside the venues. So if a venue gets deleted...all of its events do too!


###user###
(TODO)

